# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Database Configuration (for Docker deployments)
# This is the path INSIDE the container - usually don't change this
DATABASE_PATH=/app/data/telegram_note.db

# Data Directory Configuration (for deploy.sh script)
# This is where your database will be stored on the HOST machine
# Uncomment and set to your preferred location:
# DATA_DIR=~/data/telegram-note-bot
# DATA_DIR=/opt/telegram-note/data
# If not set, defaults to ./data (relative to where deploy.sh is run)

# Web App Configuration
# For local development with ngrok: https://your-subdomain.ngrok-free.app
# For production: https://yourdomain.com
WEBAPP_BASE_URL=http://localhost:8000
WEBAPP_PORT=8000

# Timezone Configuration (for Docker)
# Set to your local timezone to ensure reminders fire at correct times
# Common values: Asia/Seoul, America/New_York, Europe/London, etc.
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Asia/Seoul
